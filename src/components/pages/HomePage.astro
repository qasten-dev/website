---
import Layout from "@/layouts/Layout.astro";
import { getSafeCurrentLocale, getTranslations } from "@/utils/i18n";
import { team } from "@/utils/team";
import { Image } from "astro:assets";

const currentLocale = getSafeCurrentLocale(Astro.currentLocale);
const t = getTranslations(currentLocale);
const p = t.profiles;
const farouk = team.farouk;
const achraf = team.achraf;

const sectionRule = "border-t border-border/90";
---

<Layout title={t.meta.title} description={t.meta.description}>
  <main class="w-full">
    <section class="relative overflow-hidden" aria-labelledby="hero-heading">
      <div
        class="pointer-events-none absolute inset-0 opacity-[0.35]"
        aria-hidden="true"
        style={{
          backgroundImage: `
            radial-gradient(circle at 50% 35%, rgb(79 142 247 / 0.09) 0%, transparent 55%),
            linear-gradient(rgba(13 13 15 / 0.035) 1px, transparent 1px),
            linear-gradient(90deg, rgba(13 13 15 / 0.035) 1px, transparent 1px)`,
          backgroundSize: "100% 100%, 32px 32px, 32px 32px",
        }}
      >
      </div>
      <div class="container-consulting section-spacing relative">
        <h1
          id="hero-heading"
          class="text-foreground max-w-3xl text-3xl font-bold tracking-tight sm:text-4xl md:text-[2.35rem]"
        >
          {t.hero.headline}
        </h1>
        <p
          class="text-muted-foreground mt-6 max-w-3xl text-base leading-[1.65] sm:text-lg"
        >
          {t.hero.subline}
        </p>
        <div class="mt-10 flex flex-col gap-3 sm:flex-row sm:flex-wrap">
          <a
            href="#farouk"
            class="border-primary text-primary hover:bg-primary hover:text-primary-foreground inline-flex min-h-11 items-center justify-center rounded-lg border-2 px-6 py-2.5 text-sm font-semibold transition-colors"
          >
            {t.hero.ctaEngineering}
          </a>
          <a
            href="#achraf"
            class="border-primary text-primary hover:bg-primary hover:text-primary-foreground inline-flex min-h-11 items-center justify-center rounded-lg border-2 px-6 py-2.5 text-sm font-semibold transition-colors"
          >
            {t.hero.ctaSecurity}
          </a>
        </div>
      </div>
    </section>

    <div
      class="border-border/80 container-consulting border-t py-10 text-center"
    >
      <h2 class="text-foreground text-lg font-bold tracking-tight sm:text-xl">
        {t.meetTheTeam}
      </h2>
    </div>

    <section
      id="farouk"
      class:list={[sectionRule, "scroll-mt-24"]}
      aria-labelledby="farouk-heading"
    >
      <div class="container-consulting section-spacing">
        <p
          class="text-primary mb-8 text-xs font-semibold tracking-[0.14em] uppercase md:mb-10"
        >
          {p.pillarEngineering}
        </p>
        <div class="grid gap-12 md:grid-cols-2 md:gap-14">
          <div class="flex flex-col gap-6">
            <Image
              src={farouk.image}
              alt={p.farouk.name}
              class="border-border aspect-square w-full max-w-xs rounded-lg border object-cover"
              widths={[320, 480, 640]}
              sizes="(max-width: 768px) 100vw, 320px"
            />
            <div>
              <h2
                id="farouk-heading"
                class="text-foreground text-2xl font-bold tracking-tight"
              >
                {p.farouk.name}
              </h2>
              <p class="text-primary mt-1 text-sm font-semibold tracking-wide">
                {p.farouk.title}
              </p>
            </div>
            <div
              class="text-muted-foreground space-y-4 text-sm leading-[1.65] sm:text-base"
            >
              {p.farouk.bio.map((para) => <p>{para}</p>)}
            </div>
            <ul
              class="border-primary/70 text-muted-foreground space-y-2 border-l-2 py-0.5 pl-4 text-sm sm:text-base"
            >
              {p.farouk.credentials.map((line) => <li>{line}</li>)}
            </ul>
            <div
              class="border-primary/30 bg-primary/[0.06] ring-primary/15 rounded-lg border p-4 shadow-sm ring-1"
            >
              <p class="text-foreground/90 text-sm leading-relaxed font-medium">
                {p.farouk.contactCta}
              </p>
              <div class="mt-4 flex flex-wrap items-center gap-x-6 gap-y-2">
                <a
                  href={`mailto:${farouk.email}`}
                  class="text-primary hover:text-primary/85 inline-flex items-center gap-2 text-sm font-semibold transition-colors"
                >
                  <svg
                    class="h-4 w-4 shrink-0"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    aria-hidden="true"
                  >
                    <path
                      d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                    ></path>
                    <polyline points="22,6 12,13 2,6"></polyline>
                  </svg>
                  {p.linkEmail}
                </a>
                <a
                  href={farouk.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-primary hover:text-primary/85 inline-flex items-center gap-2 text-sm font-semibold transition-colors"
                >
                  <svg
                    class="h-4 w-4 shrink-0"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    aria-hidden="true"
                  >
                    <path
                      d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"
                    ></path>
                  </svg>
                  {p.linkLinkedin}
                </a>
              </div>
            </div>
          </div>
          <div class="flex flex-col gap-6">
            <p
              class="text-primary text-xs font-semibold tracking-[0.14em] uppercase"
            >
              {p.servicesLabel}
            </p>
            <ul class="flex flex-col gap-3">
              {
                p.farouk.services.map((item) => (
                  <li class="expertise-card border-border bg-card rounded-lg border px-4 py-4">
                    <h3 class="text-foreground text-base font-semibold">
                      {item.name}
                    </h3>
                    <p class="text-muted-foreground mt-2 text-sm leading-relaxed">
                      {item.description}
                    </p>
                  </li>
                ))
              }
            </ul>
            <ul class="flex flex-wrap gap-2">
              {
                p.farouk.skillTags.map((tag) => (
                  <li class="skill-chip">{tag}</li>
                ))
              }
            </ul>
          </div>
        </div>
      </div>
    </section>

    <section
      id="achraf"
      class:list={[sectionRule, "scroll-mt-24"]}
      aria-labelledby="achraf-heading"
    >
      <div class="container-consulting section-spacing">
        <p
          class="text-primary mb-8 text-xs font-semibold tracking-[0.14em] uppercase md:mb-10"
        >
          {p.pillarSecurity}
        </p>
        <div class="grid gap-12 md:grid-cols-2 md:gap-14">
          <div class="order-2 flex flex-col gap-6 md:order-1">
            <p
              class="text-primary text-xs font-semibold tracking-[0.14em] uppercase"
            >
              {p.servicesLabel}
            </p>
            <ul class="flex flex-col gap-3">
              {
                p.achraf.services.map((item) => (
                  <li class="expertise-card border-border bg-card rounded-lg border px-4 py-4">
                    <h3 class="text-foreground text-base font-semibold">
                      {item.name}
                    </h3>
                    <p class="text-muted-foreground mt-2 text-sm leading-relaxed">
                      {item.description}
                    </p>
                  </li>
                ))
              }
            </ul>
            <ul class="flex flex-wrap gap-2">
              {
                p.achraf.skillTags.map((tag) => (
                  <li class="skill-chip">{tag}</li>
                ))
              }
            </ul>
          </div>
          <div class="order-1 flex flex-col gap-6 md:order-2">
            <Image
              src={achraf.image}
              alt={p.achraf.name}
              class="border-border aspect-square w-full max-w-xs rounded-lg border object-cover"
              widths={[320, 480, 640]}
              sizes="(max-width: 768px) 100vw, 320px"
            />
            <div>
              <h2
                id="achraf-heading"
                class="text-foreground text-2xl font-bold tracking-tight"
              >
                {p.achraf.name}
              </h2>
              <p class="text-primary mt-1 text-sm font-semibold tracking-wide">
                {p.achraf.title}
              </p>
            </div>
            <div
              class="text-muted-foreground space-y-4 text-sm leading-[1.65] sm:text-base"
            >
              {p.achraf.bio.map((para) => <p>{para}</p>)}
            </div>
            <ul
              class="border-primary/70 text-muted-foreground space-y-2 border-l-2 py-0.5 pl-4 text-sm sm:text-base"
            >
              {p.achraf.credentials.map((line) => <li>{line}</li>)}
            </ul>
            <div
              class="border-primary/30 bg-primary/[0.06] ring-primary/15 rounded-lg border p-4 shadow-sm ring-1"
            >
              <p class="text-foreground/90 text-sm leading-relaxed font-medium">
                {p.achraf.contactCta}
              </p>
              <div class="mt-4 flex flex-wrap items-center gap-x-6 gap-y-2">
                <a
                  href={`mailto:${achraf.email}`}
                  class="text-primary hover:text-primary/85 inline-flex items-center gap-2 text-sm font-semibold transition-colors"
                >
                  <svg
                    class="h-4 w-4 shrink-0"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    aria-hidden="true"
                  >
                    <path
                      d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                    ></path>
                    <polyline points="22,6 12,13 2,6"></polyline>
                  </svg>
                  {p.linkEmail}
                </a>
                <a
                  href={achraf.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-primary hover:text-primary/85 inline-flex items-center gap-2 text-sm font-semibold transition-colors"
                >
                  <svg
                    class="h-4 w-4 shrink-0"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    aria-hidden="true"
                  >
                    <path
                      d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"
                    ></path>
                  </svg>
                  {p.linkLinkedin}
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>
